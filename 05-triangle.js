// Declare an empty string to build the triangle
let triangle = "";

// Loop 7 times to create the triangle
for (let i = 1; i <= 7; i++) {
    triangle += "#"; // Add a "#" to the string in each iteration
    console.log(triangle); // Print the current state of the triangle
}
